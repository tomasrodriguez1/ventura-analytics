# Bug Report

## Resumen

Descripción breve del bug.

## Dónde ocurre

- URL / página: (ej. `/consultor-ia/`, `/blog/{slug}/`)
- Entorno: local / preview / producción
- Navegador / dispositivo:

## Pasos para reproducir

1.
2.
3.

## Comportamiento esperado

## Comportamiento actual

## Evidencia

Screenshots, logs de consola del navegador (buscar prefijo `[API]` si es del Consultor IA), o el código de error devuelto por `getErrorMapping()` si aplica.

## Hipótesis de causa raíz

Archivo(s) probablemente involucrados (ej. `src/lib/api.ts`, `src/lib/zalantosSession.ts`, un componente específico).

## Severidad

- [ ] Crítico (sitio caído o flujo de leads roto)
- [ ] Alto (feature principal rota, con workaround)
- [ ] Medio (afecta UX pero no bloquea)
- [ ] Bajo (cosmético)

## Gaps / información faltante

Marcar con `GAP:` cualquier dato que falte para diagnosticar (ej. acceso a logs del Cloudflare Worker, que vive fuera de este repo).
